class Notebook < ApplicationRecord
    # has_many :items , class_name: "Notebook"
    
    # belongs_to :notes, class_name: "Notebook"
    # scope :notes, -> { where(type: 'Note') }
    # scope :items, -> { where(type: 'Item') }
end
